import { FilterVaultPipe } from './filter-vault.pipe';

describe('FilterVaultPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVaultPipe();
    expect(pipe).toBeTruthy();
  });
});
